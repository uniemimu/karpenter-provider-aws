---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: extensions.karpenter.sh
spec:
  group: karpenter.sh
  names:
    categories:
    - karpenter
    kind: InstanceTypeExtension
    listKind: InstanceTypeExtensionList
    plural: extensions
    singular: instancetypeextension
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: InstanceTypeExtension is the Schema for the InstanceTypeExtension
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceTypeExtensionSpec is the top level instance type
              extension specification.
            properties:
              extensions:
                items:
                  description: Extension is a single extension specification.
                  properties:
                    extendedResources:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: ResourceList is a set of (resource name, quantity)
                        pairs.
                      type: object
                    instanceNameRegex:
                      type: string
                    requiredNodePoolLabels:
                      additionalProperties:
                        type: string
                      type: object
                  required:
                  - extendedResources
                  - instanceNameRegex
                  type: object
                type: array
            required:
            - extensions
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
